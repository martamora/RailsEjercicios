class Procesador
  def initialize (cache, nucleo, velocidad, tipo)
    @cache = cache
    @nucleo = nucleo
    @velocidad = velocidad
    @tipo = tipo
  end
  
def to_s
  "#{cache}, #{nucleo}, #{velocidad}, #{tipo}"
end
end